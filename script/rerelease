# Checkout the release branch
git checkout releases/latest
# Get this branch even with main so the latest changes are included
git reset --hard main
# Rerun the checks and rebuild the TypeScript and ncc distribution
npm run build
# Add the dist
git add .
# Commit
git commit -m "latest"
# Force push to overwrite any previous commits for this branch
git push -f origin releases/latest
# Remove previously pushed tags (latest will be unusable momentarily until you re-push)
git push origin :refs/tags/latest
# Re-apply the tag
git tag -fa latest -m "latest"
# Push everything
git push origin latest
# Checkout main again
git checkout main
